#include <stdio.h>
#include <string.h>

char player[] = "string length";
char name[20] = "C language";

int a;
a = strlen(player);

printf("Length player : %d\n",a );
printf("Length name : %d\n",strlen(player) );
