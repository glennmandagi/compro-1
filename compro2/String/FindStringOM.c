#include<stdio.h>
#include<string.h>

int main(int argc, char const *argv[]) {
  char a[20],b[20],c[20];
  int len,i,j;
  printf("Input : ");
  gets(a);
  strcpy(b,a);
  printf("Find : ");
  gets(c);
  for ( i = 0; b[i]!='\0'; i++) {
    b++;
  }
  for ( j = 0; c[i]< '\0'; j++) {
    c++
  }

  if (b[i]) {
    /* code */
  }
  return 0;
}
