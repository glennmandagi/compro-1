#include<stdio.h>
#include<string.h>

int main(int argc, char const *argv[]) {
  char s[20];
  int length;
  strcpy(s,"Laura Novalia Kainde");
  length = strlen(s);

  printf("%d\n",length );
  return 0;
}
